#! frontend-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: frontend
    namespace: inventry-management-system
    labels:
        com.docker.compose.project: inventry_management_system
        com.docker.compose.service: frontend
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: inventry_management_system
            com.docker.compose.service: frontend
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: inventry_management_system
                com.docker.compose.service: frontend
                com.docker.compose.network.default: "true"
        spec:
            restartPolicy: unless-stopped
            containers:
                - name: ttrims-frontend
                  image: inventry_management_system-frontend
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: BACKEND_URL
                      value: "http://backend:5000/api"
                  ports:
                    - name: frontend-80
                      containerPort: 80
